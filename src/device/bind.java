package device;

import debug.debug;
import static util.var.*;
import net.java.games.input.Controller;
import net.java.games.input.ControllerEnvironment;

public class bind {
    public static void bind(){
        try{
            
        } catch(Exception e){
            debug.error("Could not bind keys!");
        }
    }
}
